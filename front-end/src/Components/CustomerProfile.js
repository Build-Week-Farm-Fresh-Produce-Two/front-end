//LOGIN AND SEE PRODUCE IN LOCAL AREA
//FILL SHOPPING CART AND PLACE ORDER

import React from 'react';

const CustomerProfile = () => {
    return (
        <div>
            <h1>Welcome Back</h1>
            <button onClick="">Shop</button>
{/* button needs to redirect to shopping page */}
        </div>
    )
}